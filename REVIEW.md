# 代码评审：以 slots + RPG 品类落地为前提

| 评审对象 | 本仓库（game-server-design v0.3 的实现） |
|---|---|
| 评审视角 | slots（含真钱/社交博彩）+ RPG 养成的研发、上线与运营 |
| 评审日期 | 2026-09-06 |
| 结论 | **架构地基可用，业务层不可上线。** 分片/fencing/交接这套骨架质量很好，可以直接承载 slots；但业务面存在两个可被任意客户端利用的送钱漏洞，且 slots 的品类核心（RNG、回合、账本、RTP、奖池、合规）**一个都不存在**。 |

---

## 0. 一句话判断

这份代码把「**难而通用**」的部分做对了 —— 分片独占、epoch fencing、分级刷盘、优雅交接，这些是事后最难补的东西，现在的实现经得起推敲。
但它把「**简单而致命**」的部分留空了：`add_currency` 直接暴露给客户端。

对一个普通 MMO，这是个待办；对 slots，这是**开服当天被搬空**。品类不同，同一份代码的风险等级完全不同 —— 这是本次评审的核心结论。

---

## 1. 评审前提：slots 与普通 MMO 的差异

原设计文档写的是通用游戏服务器，它没有错。但 slots 有四条 MMO 没有的硬约束，直接改变架构取舍：

**一、每一次 spin 都是一笔金融事务。**
不是「掉个道具丢了下次再刷」，是钱。丢一次 = 客诉 + 对账差异；重复一次 = 资损。
→ 这意味着 §6.2 的 L1「脏标记 + 5s」对下注/派彩**完全不适用**，必须全程 L0，且必须有流水。

**二、回合（round）是可中断的状态机，且监管要求必须能恢复。**
免费旋转打到一半断线，玩家重连后必须能接着打完。GLI-19 明确要求 incomplete round 可恢复。
→ 这意味着需要一个持久化的回合对象，而不是「一次请求一个结果」。

**三、事后必须能复算。**
客服说「玩家投诉这把该中没中」，需要凭 round_id 取出当时的 RNG 种子、paytable 版本、reel 停位，重新算一遍，得到位对位一致的结果。
→ 这意味着 RNG 必须可记录可复现，配置必须版本化，流水必须不可变。

**四、RTP 是要被监控和审计的生产指标。**
RTP 偏离预期不是「体验问题」，是配置错误或作弊的第一信号，也是监管核查项。
→ 这意味着投注额/派彩额要实时聚合，不能只靠离线报表。

RPG 侧相对宽松，但抽卡（gacha）与 slots 同源：也是 RNG + 概率公示 + 保底，同样需要流水与可复算。

---

## 2. P0 · 阻断性问题（不修不能上线）

### P0-1 客户端可以直接给自己加钱

`internal/gateway/dispatch.go:59` 把 `CmdAddCurrency`、`CmdAddItem` 放进了客户端可路由的白名单，
而 `internal/lobby/handlers.go:97` 的处理函数对调用来源没有任何校验：

```go
balance, ok := p.AddCurrency(req.GetCurrency(), req.GetDelta())
```

任何人构造一个 `add_currency{currency: 2, delta: 999999999}` 就能凭空造币。

**判断**：这不是「忘了加校验」，是**命令没有分级**这个结构性缺陷的表现。
`add_currency` 本来就该是内部命令（发奖、结算、GM 补单调用），它出现在客户端入口白名单里，
说明现在没有任何机制阻止一个内部命令被误挂到客户端侧。

**修法**：不是删掉这一行就完事 —— 那只是修掉了这一个实例。
要建立**命令权限分级**：每个命令在定义处标注 `Client / Internal / GM` 三级，
网关只放行 Client 级，Lobby 侧对 Internal/GM 级做二次校验（不信任网关）。
这样以后新增内部命令时，漏加白名单的默认行为是「拒绝」而不是「放行」。

### P0-2 充值接口没有支付校验，未知商品按客户端给的金额发钱

`internal/lobby/handlers.go` 的 `ProductReward`：

```go
default:
    if amount > 0 {
        return uint32(protocol.CurrencyDiamond), amount   // 客户端说多少就是多少
    }
```

客户端发 `purchase{order_id: "x", product: 999, amount: 1e9}` → 到账 10 亿钻石。
订单幂等做得很扎实（Lua + 订单键），但幂等保护的是「重复」，不是「伪造」。

**修法**：商品必须查配置表，未知商品直接拒绝；充值必须携带支付渠道回执（receipt/token），
由服务端向渠道验签后才入账。渠道验签本身是外部依赖，代码里留出接口 + 沙箱实现，
但**默认必须是拒绝**，而不是默认放行。

### P0-3 认证是空实现

`internal/gateway/dispatch.go` 的 `authenticate` 恒真，客户端报什么 uid 就是什么 uid。
真钱场景下这等于「输入别人的 uid 就能登录别人账号」。

**修法**：改为签名 token 校验（HMAC + 过期时间 + uid 绑定），并留出对接账号服务的接口。
默认拒绝无效 token，而不是默认放行。

### P0-4 没有 RNG

全仓库没有任何游戏随机数来源。`math/rand` 只在分片洗牌里用了一次（那个场景无所谓）。
slots 的**全部**内容就是 RNG，这一块是空的。

而且不能随手用 `math/rand`：
- 它是可预测的（已知几个输出可反推状态），slots 用它等于把 paytable 送人；
- 它无法复算（没有记录种子的机制），事后对不了账。

**修法**：独立的 `pkg/rng`，crypto/rand 播种、ChaCha8 流、**每回合记录种子**，
提供「用同一颗种子复现整局」的能力。这是审计与客服的地基。

---

## 3. P1 · 品类核心缺失（首发前必须有）

### P1-1 没有回合（round）概念

现在的模型是「一次请求 → 一次应答」。slots 需要的是：

```
下注 → 开回合 → 主旋转 → [免费旋转 × N] → 结算 → 关回合
        ↑ 中间任何一步断线，重连后必须从这里继续
```

**修法**：`Round` 对象持久化在玩家数据里（L0 级别），登录时若存在未结算回合，
在 `LoginResp` 里带回给客户端。这既是体验需求，更是监管硬要求。

### P1-2 没有账本

现在钱的变动只改玩家 base 模块，覆盖式写入。也就是说：
**余额是多少能查到，为什么是这个数查不到。**

客服要查「昨天 21:00 那把为什么扣了 500」，无从查起；财务对账无从做起。

**修法**：账本必须与余额变更**在同一个 Lua 里原子写入** —— 否则一定会出现
「扣了钱没流水」或「有流水没扣钱」，而这两种在事后都无法区分是 bug 还是欺诈。
存储用 Redis Stream（append-only，正好与玩家键同 hash tag，可以进同一个 Lua），
再留一个导出钩子把流水搬去长期存储。

设计文档 §1 写明「本期不引入 MySQL」，所以这里不引入；
但要说清楚：**Redis Stream 是过渡方案**，真钱上线前账本必须落到不可篡改的长期存储。

### P1-3 paytable 没有配置化与版本化

`ProductReward`、`LevelUpExp`、`QuestTarget`、`Stackable` 全是硬编码的 `switch`。
对 slots 更致命的是：paytable 一旦调整，历史回合就无法复算了 —— 因为没人知道当时用的是哪一版。

**修法**：配置表统一加载 + 内容哈希版本号，每个回合记录 `paytable_version`。
调参走配置发布，不走发版。

### P1-4 没有 RTP 监控

**修法**：按 `游戏 × paytable 版本` 实时聚合投注额与派彩额，暴露 RTP 指标，
并与配置的理论 RTP 做偏差告警。同时在测试里用蒙特卡洛把理论 RTP 钉死 ——
reel strip 改错一个符号，RTP 可能从 96% 跳到 130%，这种错误必须在 CI 拦住，而不是上线后靠报表发现。

### P1-5 没有 Jackpot

累积奖池是 slots 的核心玩法，也是最典型的**全局唯一强一致状态**。

**修法**：奖池余额用 Redis 原子计数器（`INCRBY` 注入，Lua 原子清零派彩），
World 服（已有选主）负责判定与广播。
注意不要走「每次 spin 发一个 job 给 World」—— 那是把一个原子加法做成了分布式事务，
既慢又多一个失败点。

### P1-6 没有责任游戏（RG）合规

会话时长、单日投注限额、自我排除、冷静期 —— 一条都没有。
多数司法辖区这是发牌照的前置条件。

**修法**：在下注前的统一检查点做拦截，限额状态随玩家数据持久化，跨日按配置时区重置。

### P1-7 RPG 侧：抽卡没有保底

抽卡与 slots 同源。没有保底（pity）的抽卡在今天基本不可发行，
且概率与保底规则多地要求公示，公示了就必须与实现一致 —— 又回到「可复算」。

---

## 4. P2 · 改进项（不阻断，但会在运营期咬人）

| 编号 | 问题 | 判断 |
|---|---|---|
| P2-1 | L0 每笔一次 Redis 往返，autoplay 下吞吐受限 | 10 万在线、3 秒一次 spin ≈ 33k 次/秒同步 Lua，单 Redis 吃紧。**group commit**（同分片攒批一次 pipeline）可提升一个数量级，代价是增加几毫秒延迟 —— 对 slots 完全可接受 |
| P2-2 | `maxWaitersPerPlayer = 64` | autoplay + 网络抖动时会误伤正常玩家。应与 L0 攒批一起调 |
| P2-3 | 明文 TCP | 真钱必须 TLS。属于部署层，代码里留出开关 |
| P2-4 | 指标缺运营维度 | 缺 RTP、hold%、spin 速率、奖池水位、回合时长、限额触发次数 |
| P2-5 | 多币种 | `CurrencyType` 是写死的枚举，真钱多地区发行需要币种 + 精度（**绝不能用 float**）|
| P2-6 | 雪花容量 | 一次 spin 要 round_id + 多条流水 ID，4096/ms/节点需按峰值复核 |
| P2-7 | 内网 NATS 无认证 | 能连上 NATS 就能发内部命令。P0-1 的分级在 Lobby 侧做二次校验后可缓解，但生产仍需 NATS 账号隔离 |

---

## 5. 明确不改的部分（以及为什么）

评审的价值一半在于指出该改什么，另一半在于拦住不该改的。

- **分片模型、epoch fencing、handoff、分级刷盘 —— 不动。**
  这套东西的实现质量高于业界平均水平，尤其 fencing 的失效前提（撞号）被识别并堵住了，
  这个洞很多线上系统都还漏着。slots 直接受益：回合状态与余额天然获得脑裂保护。

- **不引入 MySQL。** 遵循原设计的非目标。账本用 Redis Stream + 导出钩子过渡，
  并在文档里写明这是过渡方案，不假装它已经是审计级存储。

- **不重做 Room/Match。** slots 用不上，但 RPG 的 PvP 用得上，保留无害。

- **不改 `p.busy` 排队机制。** 它正确实现了「Redis 未确认前内存不变」，
  这对 slots 是必须的。要优化的是攒批，不是去掉这个保证。

---

## 6. 修改清单

按依赖顺序实施，每步保持可编译可测：

| # | 内容 | 落点 |
|---|---|---|
| 1 | 命令权限分级，网关按级放行 + Lobby 二次校验 | `pkg/protocol`、`pkg/authz`、gateway、lobby |
| 2 | 移除客户端造币入口；充值改为需渠道回执 | lobby、`pkg/payment` |
| 3 | 登录 token 签名校验 | `pkg/authn`、gateway |
| 4 | 可审计 RNG（记录种子、可复现） | `pkg/rng` |
| 5 | 配置表 + 内容哈希版本 | `pkg/gameconf` |
| 6 | 账本：与余额同 Lua 原子写入 | `pkg/ledger`、`store` |
| 7 | slots 引擎：reel/paytable/payline/free spin | `internal/slots` |
| 8 | 回合状态机 + 断线恢复 | lobby、proto |
| 9 | Jackpot：原子注入 + 选主派彩 | `pkg/jackpot`、world |
| 10 | 责任游戏限额 | `pkg/rg` |
| 11 | 抽卡保底 | `internal/lobby/gacha.go` |
| 12 | RTP/运营指标 | `pkg/metrics` |
| 13 | 蒙特卡洛 RTP 回归测试 | `internal/slots` |

---

## 7. 验收标准

改完后这些必须成立，且有测试覆盖：

1. 客户端发 `add_currency` 被拒绝（权限），即使绕过网关直连 NATS 也被 Lobby 拒绝。
2. 未知商品的充值被拒绝；无有效回执的充值被拒绝。
3. 同一颗种子 + 同一 paytable 版本，重放任意历史回合得到位对位一致的结果。
4. 蒙特卡洛 1000 万次 spin，实测 RTP 落在配置理论值 ±0.5% 内。
5. 免费旋转进行中强杀进程，重启后玩家仍能取回剩余次数并打完。
6. 每一笔余额变动都有且仅有一条对应流水，随机抽查对账无差额。
7. 奖池：并发注入后总额等于各笔之和；中奖派彩后奖池归零且只归零一次。
8. 触发日限额后下注被拒绝，跨日（按配置时区）自动恢复。
9. 抽卡保底：连续未出货达到保底次数时必定出货。
